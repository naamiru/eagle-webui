{
  "name": "eagle-webui-proxy",
  "version": "1.0.0",
  "description": "Eagle API proxy for Eagle WebUI",
  "main": "app.ts",
  "scripts": {
    "start": "tsx src/server.ts",
    "dev": "tsx --watch src/server.ts",
    "test": "node --test --test-only --import tsx 'src/**/*.test.ts'",
    "lint": "biome check .",
    "lint:fix": "biome check --write ."
  },
  "keywords": [],
  "author": "naamiru",
  "license": "The Unlicense",
  "devDependencies": {
    "@types/node": "^24.2.1",
    "fastify-tsconfig": "^3.0.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "fastify": "^5.4.0",
    "fastify-plugin": "^5.0.1",
    "pino-pretty": "^13.1.1"
  }
}
