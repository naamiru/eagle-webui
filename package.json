{
  "name": "eagle-webui",
  "version": "1.0.0",
  "description": "web interface for the Eagle image viewer",
  "repository": {
    "type": "git",
    "url": "https://github.com/naamiru/eagle-webui.git"
  },
  "license": "The Unlicense",
  "author": "naamiru",
  "type": "module",
  "scripts": {
    "dev": "concurrently 'npm run dev -w front' 'npm run dev -w proxy'",
    "test": "npm run test -w front -- --run && npm run test -w proxy -- --run"
  },
  "workspaces": [
    "front",
    "proxy"
  ],
  "devDependencies": {
    "@biomejs/biome": "2.1.4",
    "concurrently": "^9.2.0"
  }
}
